# 接口与外部契约定义（Runtime / Template v2.0）

## 1. 文档定位

本文档是《[Langchain重构方案大纲](./Langchain重构方案大纲.md)》的外部契约配套文档，用于锁定前端、Python 宿主、TypeScript runtime、provider adapter 与模板作者之间的正式接口边界。

两份文档分工如下：

- 《Langchain 重构方案大纲》定义内部架构、状态流、Tool 责任链、投影层与物化链路。
- 本文档定义前端可见模型、runtime 输入边界、Web API 返回 shape、模板外部合同与诊断摘要边界。

默认决策如下：

- Template 外部主契约继续沿用 `TemplateContract`，`schema_version = "2.0"`。
- 前端输出层采用“增强可审计”模型。
- 不直接向前端暴露完整内部调试对象。
- 当前活动实现以 `src/langgraph-ts/` 为准；legacy Python Web API 与旧前端只视为兼容承载层。

当前代码锚点如下：

- runtime 输入 / 状态契约：`src/langgraph-ts/runtime/contracts.ts`
- runtime graph：`src/langgraph-ts/runtime/graph.ts`
- provider adapter 与强约束 tool schema：`src/langgraph-ts/model/provider-adapter.ts`
- 文档投影类型：`src/langgraph-ts/contracts/document-contracts.ts`
- 本地 Web API 兼容层：`archive/legacy/python-host/gui/web_api.py`

## 2. 可见性边界

前端正式可见信息固定为“增强可审计”，即：

- 能看到任务状态、步骤、错误、告警、摘要性诊断与输出引用。
- 能看到分类命中、冲突、未匹配、baseline 来源与最终应用字段的摘要。
- 能看到预算命中、降级步骤、批次拆分与 provider / runtime 结构化失败摘要。
- 能看到足够支撑 UI 渲染与人工复核的审计信息。
- 不能直接依赖内部 AST、完整 patch history、原始 selector 分析或全量 prompt 中间态。

明确不直接暴露以下内部对象：

- 完整 `document_ast`
- 完整 patch plan / patch history
- 原始 `SelectorTargetAnalysis`
- 完整 `chat_projection` / `template_projection` 原文
- 全量内部 prompt、classifier 中间态、模型诊断原文

## 3. 统一外部类型

以下类型是前端与 Web API 的统一外部模型。字段名以 JSON 返回为准。

### 3.1 通用类型

```ts
export type JobStatus = "queued" | "running" | "waiting_user" | "completed" | "failed";
export type StepStatus = "queued" | "running" | "completed" | "failed";

export interface ProgressStep {
  id: string;
  title: string;
  status: StepStatus;
  detail?: string;
  startedAt?: number;
  updatedAt?: number;
}

export interface Warning {
  code: string;
  scope: string;
  message: string;
  paragraphIds?: string[];
  diagnostics?: Record<string, unknown>;
}

export interface DiagnosticSummary {
  errorCode?: string;
  message: string;
  retryable?: boolean;
  paragraphIds?: string[];
  semanticKey?: string;
  reason?: string;
}

export interface ArtifactRef {
  kind: string;
  label: string;
  path?: string;
  contentType?: string;
  metadata?: Record<string, unknown>;
}

export interface ApiErrorBody {
  error: {
    code?: string;
    message: string;
    retryable?: boolean;
    stage?: string;
    details?: Record<string, unknown>;
    jobId?: string;
    turnRunId?: string;
    outputPath?: string;
    status?: JobStatus;
  };
}

export interface ProjectionIntent {
  focus_regex_probe?: string;
  chat_text_budget?: number;
  template_text_budget?: number;
  template_batch_budget?: number;
  chat_neighbor_window?: number;
  template_local_context_window?: number;
}
```

说明：

- `ProjectionIntent` 是当前 runtime / CLI / 测试入口的正式输入模型。
- 宿主层若向 LangGraph runtime 透传投影偏好，必须保持字段语义与命名一致，不应自行改写。

### 3.2 会话与消息模型

```ts
export interface SessionSummary {
  sessionId: string;
  title: string;
  createdAt: number;
  updatedAt: number;
}

export interface AttachedDocumentSummary {
  path?: string;
  name?: string;
}

export interface ChatMessage {
  messageId: string;
  sessionId: string;
  role: "user" | "assistant" | "system";
  content: string;
  attachments?: FileAttachment[];
  clientCreatedAt?: number;
}

export interface SessionDetail {
  sessionId: string;
  title: string;
  messages: ChatMessage[];
  attachedDocument: AttachedDocumentSummary | null;
}

export interface MessageJobError {
  code?: string;
  message: string;
  stage?: string;
  retryable?: boolean;
  details?: Record<string, unknown>;
}

export interface MessageJobSnapshot {
  jobId: string;
  sessionId: string;
  turnRunId?: string;
  status: JobStatus;
  acceptedAt: number;
  updatedAt: number;
  summary: string;
  steps: ProgressStep[];
  error?: MessageJobError | null;
  warnings?: Warning[];
}
```

说明：

- `SessionSummary` 对应左侧会话列表。
- `SessionDetail` 是聊天工作区完整加载模型。
- `MessageJobSnapshot` 是 `/messages/async` 与 `/message-jobs/{jobId}` 的统一轮询对象。

### 3.3 模板相关模型

```ts
export interface TemplateMetaSummary {
  id: string;
  name: string;
  version: string;
  description?: string;
  tags?: string[];
  path: string;
}

export interface TemplateDocumentSummary {
  fileName: string;
  uploadedPath: string;
}

export interface TemplateDocumentMeta {
  fileName?: string;
  uploadedPath?: string;
  paragraphCount?: number;
  tableCount?: number;
  imageCount?: number;
}

export interface SemanticClassificationSummary {
  matchedSemanticKeys: string[];
  unmatchedParagraphIds: string[];
  conflictParagraphIds: string[];
  overallConfidence?: number;
}

export interface SemanticAlignmentSummary {
  targetParagraphIds: string[];
  baselineSourceParagraphIds: string[];
  appliedFields: string[];
  confidenceState: "high" | "medium" | "low" | "unknown";
}

export interface ValidationSummary {
  passed: boolean;
  issueCount: number;
  warningCount: number;
  issues?: DiagnosticSummary[];
}

export interface TemplateJobError {
  code?: string;
  message: string;
  stage?: string;
  retryable?: boolean;
  details?: Record<string, unknown>;
}

export interface TemplateJobSnapshot {
  jobId: string;
  sessionId: "templates";
  status: JobStatus;
  acceptedAt: number;
  updatedAt: number;
  summary: string;
  steps: ProgressStep[];
  error?: TemplateJobError | null;
  warnings?: Warning[];
  outputPath?: string;
  templateMeta?: TemplateMetaSummary;
  documentMeta?: TemplateDocumentMeta;
  classificationSummary?: SemanticClassificationSummary;
  validationSummary?: ValidationSummary;
  alignmentSummary?: SemanticAlignmentSummary;
  diagnostics?: DiagnosticSummary[];
  artifacts?: ArtifactRef[];
  debug?: {
    refinementSummary?: Array<{
      paragraphId: string;
      firstPass?: Record<string, unknown>;
      secondPass?: Record<string, unknown>;
      outcome: string;
    }>;
  };
}
```

说明：

- `TemplateJobSnapshot` 是模板任务的正式运行态契约。
- `debug` 仅保留兼容窗口，不应继续扩成全量内部对象出口。
- `templateMeta`、`classificationSummary`、`validationSummary`、`alignmentSummary`、`artifacts` 是下一阶段应补齐的稳定外部字段。

## 4. 前端可见信息的正式边界

前端至少应能稳定拿到以下信息：

- 模板列表：`id`、`name`、`version`、`description`、`tags`、`path`
- 导入文档结果：`fileName`、`uploadedPath`
- 模板任务状态：`jobId`、`status`、`summary`、`acceptedAt`、`updatedAt`、`steps`、`outputPath`
- 告警与诊断：`warnings`、分类冲突摘要、低置信摘要、refinement summary、最终 `appliedFields`
- 审计摘要：命中的语义块、冲突段 ID、未匹配段 ID、baseline 来源段 ID、最终同步字段集合

前端不应依赖以下内部细节：

- 完整观察对象全文
- `TemplateClassificationResult` 的全部内部字段
- 完整 patch set、write plan、materialize 中间产物
- 只在 CLI stderr 中出现的临时诊断格式

## 5. Web API 契约

### 5.1 通用约束

- JSON API 统一使用 `application/json; charset=utf-8`。
- 失败响应统一返回 `ApiErrorBody`。
- 异步任务接口返回 `202 Accepted`。
- 轮询接口在 `queued`、`running`、`waiting_user`、`completed`、`failed` 下都必须返回稳定 shape。

`status` 语义固定如下：

- `queued`：任务已受理，尚未进入实际执行。
- `running`：任务已开始执行，允许继续轮询。
- `waiting_user`：任务暂停，等待用户补充信息或确认。
- `completed`：任务完成，结果稳定。
- `failed`：任务终止，返回错误与诊断摘要。

`waiting_user` 约束如下：

- 仅用于需要前端继续采集输入的场景。
- 不能与 `failed` 混用表达“需要人工排查”。
- 轮询对象必须保留同一个 `jobId`，由后续用户动作继续推进。

### 5.2 会话接口

#### `GET /api/sessions`

- 响应：`{ sessions: SessionSummary[] }`

#### `POST /api/sessions`

- 请求体：

```json
{ "sessionId": "chat-optional" }
```

- 响应：`{ session: SessionDetail }`

#### `GET /api/sessions/{sessionId}`

- 响应：`{ session: SessionDetail }`

#### `PATCH /api/sessions/{sessionId}`

- 请求体：

```json
{ "title": "新的会话标题" }
```

- 响应：`{ session: SessionDetail }`

#### `DELETE /api/sessions/{sessionId}`

- 响应：

```json
{ "deletedSessionId": "chat-1234" }
```

### 5.3 文档绑定与消息接口

#### `POST /api/sessions/{sessionId}/attach-document`

- `Content-Type`：`multipart/form-data`
- 响应：

```json
{
  "session": { "...": "SessionDetail" },
  "uploadedPath": "D:/.../upload.docx"
}
```

说明：

- 当前实现返回 `uploadedPath`，但正式前端模型应继续以 `session.attachedDocument` 为主。

#### `POST /api/sessions/{sessionId}/messages`

- 请求体：

```json
{ "content": "请分析文档" }
```

- 成功响应：`{ session: SessionDetail }`
- 超时但任务仍在后台执行时，返回：
  - HTTP `504`
  - `error.code = "E_SYNC_REQUEST_TIMEOUT"`
  - `error.retryable = true`
  - 同时携带 `jobId`、`turnRunId`、`status`

#### `POST /api/sessions/{sessionId}/messages/async`

- 请求体：

```json
{ "content": "请分析文档" }
```

- 响应：`{ job: MessageJobSnapshot }`

#### `GET /api/sessions/{sessionId}/message-jobs/{jobId}`

- 响应：

```json
{
  "job": { "...": "MessageJobSnapshot" },
  "session": { "...": "SessionDetail", "optional": true }
}
```

说明：

- `session` 仅在后端已经拿到稳定会话结果时返回。
- 前端必须允许 `session` 缺失，只使用 `job` 渲染进度卡片。

### 5.4 模板接口

#### `GET /api/templates/configs`

正式目标响应：

```json
{
  "configs": [
    {
      "id": "thesis-v1",
      "name": "论文模板",
      "version": "1.0.0",
      "description": "本科论文模板",
      "tags": ["thesis", "cn"],
      "path": "D:/.../templates/论文模板.json"
    }
  ]
}
```

当前实现状态：

- 当前 `web_api.py` 仅返回 `fileName` 与 `path`。
- 下一阶段应从 `TemplateContract.template_meta` 读取并回填正式列表模型。

#### `POST /api/templates/import-document`

- `Content-Type`：`multipart/form-data`
- 响应：`{ document: TemplateDocumentSummary }`

#### `POST /api/templates/runs`

- 请求体：

```json
{
  "documentPath": "D:/.../input.docx",
  "templatePath": "D:/.../templates/sample.json"
}
```

- 响应：`{ job: TemplateJobSnapshot }`
- 任务步骤 ID 固定为：
  - `load_inputs`
  - `run_template_pipeline`
  - `validate_result`
  - `materialize_output`

#### `GET /api/templates/runs/{jobId}`

- 响应：

```json
{
  "job": { "...": "TemplateJobSnapshot" },
  "outputPath": "D:/.../output.docx"
}
```

说明：

- `outputPath` 可以与 `job.outputPath` 同时存在，供兼容层使用。
- 长期目标应以内嵌 `job.outputPath` 为主，顶层重复字段仅保留兼容期。

#### `POST /api/templates/open-output`

- 请求体：

```json
{ "outputPath": "D:/.../output.docx" }
```

- 响应：

```json
{ "ok": true }
```

### 5.5 错误体约束

模板与消息任务的 `error` / `ApiErrorBody.error` 应统一支持以下字段：

```ts
export interface StandardError {
  code?: string;
  message: string;
  stage?: string;
  retryable?: boolean;
  details?: Record<string, unknown>;
}
```

模板链路的错误至少要能区分以下类别：

- 普通执行失败
- `classification_conflict`
- 低置信或歧义导致的失败
- baseline 不可判定
- 输出文件不存在或物化失败

当前 runtime / provider 侧还应保留以下结构化失败语义：

- `E_GRAPH_RECURSION_LIMIT`
- `E_CHAT_PROJECTION_*`
- `E_TEMPLATE_PROJECTION_*`
- `E_TEMPLATE_CLASSIFICATION_*`
- `provider_protocol_error`
- `provider_read_loop_exhausted`
- `provider_tool_args_unmappable`
- `provider_reasoning_context_missing`

## 6. Template v2.0 外部合同

### 6.1 主契约不变

Template 外部主契约继续使用 `src/ts/src/templates/template-contract.ts` 中的 `TemplateContract`。当前对外锁定字段为：

- `template_meta`
- `semantic_blocks`
- `derived_semantics`
- `layout_rules`
- `patch_blocks`
- `classification_contract`
- `validation_policy`
- `style_reference`

### 6.2 字段边界

字段责任边界固定如下：

- `patch_blocks`：执行主契约，是模板真正的写入来源。
- `style_reference`：说明性与审阅辅助元数据，不是执行真源。
- `classification_contract`：模板行为约束，属于模板输入，不属于任务结果对象。
- `validation_policy`：模板校验策略，属于模板输入，不属于运行态诊断快照。

### 6.3 作者输入与运行输出分层

模板 JSON 只承载作者输入契约，不承载运行态输出。

作者输入契约包括：

- 模板元信息
- 原子语义定义
- 派生语义定义
- 布局规则
- patch 执行动作
- 分类约束
- 校验策略
- 人工可读样式说明

运行态输出与诊断结果包括：

- 分类命中结果
- 冲突段、未匹配段
- 低置信摘要
- refinement summary
- 执行结果、输出路径
- runtime warnings
- artifacts 与审计摘要

这些运行态字段不能写回模板 JSON。

### 6.4 推荐作者模型

模板作者推荐按以下顺序建模：

1. 先写 `semantic_blocks`，明确文档中的原子语义块。
2. 用 `layout_rules` 表达位置、顺序、编号和样式约束。
3. 用 `patch_blocks` 表达真正执行的修改动作。
4. 仅把 `style_reference` 作为人工说明与评审辅助。

不推荐的写法包括：

- 把运行时分类结果回填到 `classification_contract.matches`
- 把失败诊断、冲突段、未匹配段写回模板 JSON
- 用 `style_reference` 代替 `patch_blocks`

## 7. 脏文档语义回退的外部摘要

内部算法可以继续使用 `semantic_heading`、`title_like_paragraphs`、`body_style_baseline` 等机制，但前端只消费摘要对象。

正式摘要类型如下：

```ts
export interface SemanticClassificationSummary {
  matchedSemanticKeys: string[];
  unmatchedParagraphIds: string[];
  conflictParagraphIds: string[];
  overallConfidence?: number;
}

export interface SemanticAlignmentSummary {
  targetParagraphIds: string[];
  baselineSourceParagraphIds: string[];
  appliedFields: string[];
  confidenceState: "high" | "medium" | "low" | "unknown";
}

export interface TemplateDiagnosticSummary {
  errorCode?: string;
  message: string;
  retryable?: boolean;
  paragraphIds?: string[];
  semanticKey?: string;
  reason?: string;
}
```

这层摘要必须满足：

- 能支撑前端做冲突提示与人工复核。
- 能支撑审计输出 `appliedFields` 与 baseline 来源。
- 不泄漏完整内部分类 prompt 或完整结构观察对象。

## 8. 契约测试目标

接口定义阶段同时锁定以下测试目标：

1. 模板列表接口返回的模板元数据与现有 JSON 模板中的 `template_meta` 一致。
2. 模板任务轮询接口在 `queued`、`running`、`completed`、`failed` 下返回稳定 shape。
3. 模板运行失败时，错误体能区分普通失败、分类冲突、低置信和 baseline 不可判定。
4. 前端可见审计摘要中稳定包含冲突段列表、未匹配段列表、baseline 来源段列表和 `appliedFields`。
5. `TemplateWorkspace` 不读取内部 TS 对象，只依赖正式 API 类型即可完成渲染。
6. `TemplateContract v2.0` 示例模板与接口规格保持一致，不出现“文档支持但校验器不支持”的分叉。

## 9. 当前实现与目标契约的对齐状态

截至当前仓库状态，已落地部分如下：

- `TemplateContract v2.0` 已在 TypeScript 侧作为主契约存在。
- `projection_intent` 已进入 runtime 输入与状态，并真实驱动 chat / template projection 的预算、焦点和邻域策略。
- `chat_projection` 已具备预算估算、固定降级顺序、traceability 校验与结构化失败返回。
- `template_projection` 已具备分类必需字段保留、批次拆分、预算降级、顺序校验与结构化失败返回。
- `template_classifier` 已真实消费 `template_projection.batches`，`template_config.semantic_tags` 不再作为可信分类输入。
- provider `write_document` schema 已收紧为受限 `semantic_selector` 与受限语义 payload。
- provider `read -> write` 回放结果已包含 `chat_projection` / `template_projection` 摘要，而不再只返回旧 paragraph summary。
- 模板任务步骤 ID 已固定为 `load_inputs`、`run_template_pipeline`、`validate_result`、`materialize_output`。
- `TemplateJobSnapshot` 当前已返回 `jobId`、`status`、`summary`、`steps`、`error`、`warnings`、`outputPath`。
- 模板失败场景已能输出 `debug.refinementSummary` 兼容摘要。

仍待补齐的正式外部字段如下：

- 模板列表的 `id`、`name`、`version`、`description`、`tags`
- `TemplateJobSnapshot.templateMeta`
- `TemplateJobSnapshot.documentMeta`
- `TemplateJobSnapshot.classificationSummary`
- `TemplateJobSnapshot.validationSummary`
- `TemplateJobSnapshot.alignmentSummary`
- `TemplateJobSnapshot.artifacts`
- 若宿主层后续对外暴露投影审计，建议补 `projectionIntent` 与投影 / 分类 diagnostics 摘要字段，但不要直接暴露完整投影对象。

本文件定义的是下一阶段必须对齐的正式外部契约；实现可以分步补齐，但字段语义从本文件起固定。
